myFacts([]).

transitivePreds([]).

inform([]).

agentNeeded(plantOne,sendVolume).
agentNeeded(plantTwo,sendContaminant).
agentNeeded(plantZero,sendContaminant).


predSubclass(action,thing).
predSubclass(inform,predicate).


protectFunction([]).

protectArgument([]).

tasksIPerform([]).

ask([]).

wait([]).

nonFacts([]).
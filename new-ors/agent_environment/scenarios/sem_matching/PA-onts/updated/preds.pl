predicate(hasSkill(skill,agent)).
predicate(employs(job,agent)).
predicate(lastName(name,agent)).
predicate(titles(name,certificate)).
predicate(represents(thing,item)).
predicate(agent(process,agent)).
predicate(result(thing,thing)).
predicate(contestParticipant(contest,agent)).
predicate(involvedInEvent(contest,thing)).
predicate(property(agent,property)).

